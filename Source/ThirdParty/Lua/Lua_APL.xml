<?xml version="1.0" encoding="utf-8"?>
<!--GRV Controller plugin additions-->
<root xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- init section is always evaluated once per architecture -->
	<init>
		<log text="Lua init"/>
	</init>

	<!-- optional files or directories to copy to Intermediate/Android/APK -->
	<resourceCopies>
    <log text="Copying Lua files to staging"/>
    <log text="libLua Begin...... PluginDir is $S(PluginDir), BuildDir is $S(BuildDir)"/>
    
		<isArch arch="armeabi-v7a">
			<copyFile src="$S(PluginDir)/lib/android/armv7-a/release/libLua.so"
				  	dst="$S(BuildDir)/libs/armeabi-v7a/libLua.so" />
		</isArch>

    <isArch arch="arm64-v8a">
      <copyFile src="$S(PluginDir)/lib/android/arm64-v8a/release/libLua.so"
				  	dst="$S(BuildDir)/libs/arm64-v8a/libLua.so" />
    </isArch>
    
	</resourceCopies>


  <!-- optional additions to proguard -->
  <proguardAdditions>
  </proguardAdditions>

  <gameActivityImportAdditions>
  </gameActivityImportAdditions>

  <gameActivityClassAdditions>
  </gameActivityClassAdditions>

  <gameActivityOnPauseAdditions>
  </gameActivityOnPauseAdditions>

  <gameActivityOnResumeAdditions>
  </gameActivityOnResumeAdditions>

  <gameActivityOnCreateAdditions>
  </gameActivityOnCreateAdditions>

  <gameActivityOnStartAdditions>
  </gameActivityOnStartAdditions>

  <!-- optional libraries to load in GameActivity.java before libUE4.so -->
	<soLoadLibrary>
        <loadLibrary name="Lua" failmsg="Lua library not loaded and required!" />
	</soLoadLibrary>
</root>
